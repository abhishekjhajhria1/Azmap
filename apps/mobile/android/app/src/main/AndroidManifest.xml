<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- ABH works entirely offline; the internet is only for optional sync to
         your own relay. Declared because sync exists, not because the app
         needs a connection to be useful. -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:label="ABH"
        android:name="${applicationName}"
        android:icon="@drawable/ic_launcher"
        android:enableOnBackInvokedCallback="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">

            <!-- The window background before Flutter's first frame. Without
                 this a launch shows a white flash, which on a dark-themed app
                 is the most visible possible way to look unfinished. -->
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Share sheet. The capture path people actually use on a phone:
                 nobody opens a notes app to save an article, they hit Share
                 from wherever they're reading.

                 `text/*`, not `*/*`. Advertising every MIME type puts ABH in
                 the share sheet for video files it can do nothing useful
                 with, which is worse than not appearing at all. -->
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/*" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.SEND_MULTIPLE" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/*" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>

    <!-- Android 11+ package visibility: without this, opening a captured link
         in a browser silently does nothing, because the app cannot see that a
         browser exists to hand it to. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT" />
            <data android:mimeType="text/plain" />
        </intent>
    </queries>
</manifest>
