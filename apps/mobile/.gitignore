# Build output and tool state. Never the platform folders themselves — see
# android/README.md for why committing them is the point.
.dart_tool/
.packages
build/
*.iml
.flutter-plugins
.flutter-plugins-dependencies
android/local.properties

# Font binaries are fetched, not vendored. See README.
fonts/*.ttf
